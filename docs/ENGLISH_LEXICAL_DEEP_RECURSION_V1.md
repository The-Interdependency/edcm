# English lexical deep recursion v1 result

Status: `SURVIVED` for the deterministic, dictionary-bounded closed depth-one
construction. This is represented lexical evidence, not linguistic proof,
geometry, EDCM measurement validity, or canon.

The frozen producer at `195e4d6` processed the complete OEWN 2025 inventory
over all 131,094 surface gonols in the sealed direct-atomic floor. Two clean
complete runs produced byte-identical seven-file artifact sets; fail-closed
resume then revalidated the exact source, floor, UCNS producer, file set,
digests, intrinsic carrier, external bindings, and coverage ledger.

## Result

The metadata-free UCNS carrier contains 145,563 nodes and 1,005,873 ordered
typed edges. Its external evidence binding accounts for:

- all 285,546 directed distinct-member synset pairs;
- all 131,098 synset-relation occurrences, expanded to 466,789 floor-word
  edges;
- all 113,629 sense-relation occurrences, expanded to 139,202 floor-word
  edges;
- 14,469 wholly closed definitions, represented by 63,940 ordered token edges
  and 50,396 bidirectional definition/member edges.

No semantic relation occurrence or synset member pair was excluded. The
stronger claim that the existing lemma floor can reconstruct every OEWN
definition is `FALSIFIED`: 93,055 of 107,524 definitions contain at least one
of 13,759 atom types absent from the floor, totaling 330,856 missing atom
occurrences. The full rejected-definition identities, text, token gaps, and
counts remain in `coverage.json`; the rule was not weakened after observing
the outcome.

English and provenance remain external to intrinsic UCNS objects. Relation
multiplicity, source order, token order, repeated tokens, spans, alternative
definitions, and source identities are recoverable. No dictionary sense was
selected as canonical.

The exact compact receipt is
`experiments/lexical/english-lexical-deep-recursion-run-v1.json`; the authority
and nontransfer graph is
`experiments/lexical/english-lexical-deep-recursion-work-graph-v1.json`.

## hmmm

Closing the rejected definitions would require changing the existing floor
(most visibly by admitting function words and inflected forms) or explicitly
constructing another layer. This run provides no authority for either choice.
Depth beyond one, geometry, measurement validity, canonical sense selection,
and phrase/sentence/discourse semantics remain `UNRESOLVED`.
